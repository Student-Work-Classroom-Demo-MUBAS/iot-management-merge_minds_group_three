# Database connection (PostgreSQL)
DB_NAME=iot_db
DB_USER=postgres
DB_PASSWORD=coco
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres

# Full connection string (optional, for libraries that support it)
DATABASE_URL=postgres://postgres:coco@localhost:5432/iot_db

# Server settings
PORT=3000
NODE_ENV=development

# Security
JWT_SECRET=replace_with_long_random_value
BCRYPT_SALT_ROUNDS=12

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# CORS
CORS_ORIGIN=http://localhost:3000

